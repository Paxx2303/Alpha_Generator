# Skills Directory

This directory contains skills for the deerflow agent.

Skills are reusable components that can be loaded into deerflow to extend its capabilities.

## Usage

Skills in this directory will be automatically mounted to the deerflow container at `/app/skills`.

## Structure

- Each skill should be in its own subdirectory
- Skills can contain Python files, configuration files, and documentation
- Skills are loaded by deerflow at runtime