BASIC_MODEL:
  base_url: https://openrouter.ai/api/v1
  model: "openrouter/owl-alpha"
  api_key: $OPENROUTER_API_KEY
  max_retries: 2
  verify_ssl: true

# Keep web search enabled so DeerFlow can continue market / WQ-related research.
ENABLE_WEB_SEARCH: true
